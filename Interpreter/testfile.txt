var x = 10;
var y = 0;

{
var z = 10;
print z;
}

var a = "hi";
print a;

{
    print a;
}

var z = 5;
print z;

if (x > 5) 
{
    y = 100;
    print y;
}
else 
{
    y = 42;
}

print y;

print "While Loop";
var count = 0;
while (count < 3) 
{
    print count;
    count = count + 1;
}

print "For Loop";
for (var i = 0; i < 3; i = i + 1)
{
    print i;
    y = y + 1;
    print y;
}

print "testing functions";

func hey(var e, var f)
{
    return e + f;
}

var g = hey(x, y);
print g;

print hey(5, 6);

var h = hey(x, y);
print h;

print hey(x, y);

var a = "global";
{
    func showA() {
        print a;
    }
    showA();       // prints "global"
    var a = "block";
    showA();       // should still print "global" (showA's closure captured the *global* a),
}

class Empty {}
var e = Empty();
print e;

class Point {}
var p = Point();
p.x = 3;
p.y = 4;
print p.x;
print p.y;

class Greeter {
    hello() {
        print "hi";
    }
}
var g = Greeter();
g.hello();

//fails on test case because (lacks (TokenType::THIS))
class Counter {
    init() {
        this.count = 5;
    }
    increment() {
        this.count = this.count + 1;
        print this.count;
    }
}
var c = Counter();
c.increment();
c.increment();
c.increment();

class Counter {
    init() { this.count = 0; }
    increment() { this.count = this.count + 1; }
    get() { return this.count; }
}
var a = Counter();
var b = Counter();
a.increment();
a.increment();
b.increment();
print a.get();
print b.get();

class Foo {
    init() { this.x = 1; }
}
print Foo();

var foo = Foo();
print foo.init();
print foo.x;

class Animal {
    init(var name) {
        this.name = name;
    }

    speak() {
        print this.name + " makes a sound.";
    }

    describe() {
        print this.name + " is an animal.";
    }
}

class Dog < Animal {
    speak() {
        super.speak();
        print this.name + " barks.";
    }
}

var a = Animal("Generic");
a.speak();
print "---";
var d = Dog("Rex");

d.speak();    
d.describe();